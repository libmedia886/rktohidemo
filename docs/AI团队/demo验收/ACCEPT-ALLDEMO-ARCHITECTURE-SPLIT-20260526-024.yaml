acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-024"
  case_name: "ALLDEMO_STANDALONE_RETINEX_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- src/pages/page_ops.c src/pages/page_retinex_offline.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-024.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-024.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-024.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only RETINEX"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only RETINEX_OFFLINE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    retinex_standalone:
      - "RETINEX no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_retinex_run."
      - "Standalone page creates RETINEX_73 and loads EXDark samples."
      - "Probe reports module=live and samples=100."
      - "retinex_frames grows from 1 to 6 during the probe."
      - "Shutdown removes RETINEX_73 and destroys pool 6 and display pool 1."
    regression:
      - "RETINEX_OFFLINE standalone short probe continues to run with module=live."
    assets:
      - "asset-check continues to pass after the RETINEX migration."
  acceptance_limits:
    - "This stage accepts RETINEX as a standalone sample-driven module page."
    - "It does not claim realtime camera input has been reimplemented outside legacy."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
