acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-026"
  case_name: "ALLDEMO_STANDALONE_DCP_FAST_DEHAZE_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- src/pages/page_ops.c src/pages/page_cap_dehaze_offline.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-026.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-026.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-026.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only DCP_FAST_DEHAZE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only CAP_DEHAZE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    dcp_fast_dehaze_standalone:
      - "DCP_FAST_DEHAZE no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_dcp_fast_dehaze_run."
      - "Standalone page creates DCP_FAST_DEHAZE_67 with OpenCL backend."
      - "Probe reports module=live and samples=3."
      - "dcp_frames grows from 1 to 2 after sample switch."
      - "Shutdown removes DCP_FAST_DEHAZE_67 and destroys pool 7, pool 6, and display pool 1."
    regression:
      - "CAP_DEHAZE standalone short probe continues to run with module=live."
    assets:
      - "asset-check continues to pass after the DCP_FAST_DEHAZE migration."
  acceptance_limits:
    - "This stage accepts DCP_FAST_DEHAZE as a standalone sample-driven module page."
    - "It does not claim realtime camera input has been reimplemented outside legacy."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
