acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-015"
  case_name: "ALLDEMO_STANDALONE_CAP_DEHAZE_OFFLINE_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_cap_dehaze_offline.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-015.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-015.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-015.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only CAP_DEHAZE_OFFLINE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only RETINEX_OFFLINE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    cap_dehaze_offline_standalone:
      - "CAP_DEHAZE_OFFLINE no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_cap_dehaze_offline_run."
      - "Standalone page scans 3 cap_dehaze JPEG samples and creates MEDIA_CAP_DEHAZE_66."
      - "Probe reaches sample 2/3 and cap_frames=2, showing real module processing."
      - "OpenCL backend is enabled for CAP_DEHAZE."
      - "Shutdown removes CAP_DEHAZE_66 and destroys pool 7, pool 6, and display pool 1."
    regression:
      - "RETINEX_OFFLINE standalone short probe continues to run with retinex_frames=2."
    assets:
      - "asset-check continues to pass after the CAP_DEHAZE_OFFLINE migration."
  acceptance_limits:
    - "This stage accepts only the CAP_DEHAZE_OFFLINE standalone page."
    - "The realtime CAP_DEHAZE camera/bind page remains legacy proxy."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
