acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-028"
  case_name: "ALLDEMO_STANDALONE_CSC_CL_AND_CSC_RGA_PAGES"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_csc.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-028.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-028.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-028.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only CSC_CL"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only CSC_RGA"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only CONV_CL"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    csc_cl_standalone:
      - "CSC_CL no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_csc_cl_run."
      - "Standalone page creates CSC_CL_76 with OpenCL path."
      - "Probe reports processed growth from 30 to 150 and gpu perf."
      - "Shutdown removes CSC_CL_76 and destroys pool 7, pool 6, and display pool 1."
    csc_rga_standalone:
      - "CSC_RGA no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_csc_rga_run."
      - "Standalone page creates CSC_RGA_64 with RGA path."
      - "Probe reports processed growth from 30 to 150."
      - "Shutdown removes CSC_RGA_64 and destroys pool 7, pool 6, and display pool 1."
    regression:
      - "CONV_CL standalone short probe continues to run with module=live."
    assets:
      - "asset-check continues to pass after the CSC migrations."
  acceptance_limits:
    - "This stage accepts CSC_CL and CSC_RGA as standalone synthetic-input module pages."
    - "It does not claim realtime VI/VPSS/OSD/RESIZE chains have been reimplemented outside legacy."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
