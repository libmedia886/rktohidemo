acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-021"
  case_name: "ALLDEMO_STANDALONE_PANO_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_pano.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-021.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-021.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-021.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 15s ./scripts/run_alldemo.sh --only PANO"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only AVM2D"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    pano_standalone:
      - "PANO no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_pano_run."
      - "Standalone page loads sample2 PTO and six camera images."
      - "Probe uses real MEDIA_PANO path with mode=module-preview."
      - "PTO LUT is built from calib_file.pto and output size is 8378x4190."
      - "Probe reports output=1/1 and frame counter growth from 30 to 330."
      - "Shutdown removes PANO_74 and destroys pool 6, pool 7, and display pool 1."
    regression:
      - "AVM2D standalone short probe continues to run with frames=25/25 outputs=yes."
    assets:
      - "asset-check continues to pass after the PANO migration."
  acceptance_limits:
    - "This stage accepts only the PANO standalone page."
    - "Fallback mode exists but this run validated the real module-preview path."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
