acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-014"
  case_name: "ALLDEMO_STANDALONE_RETINEX_OFFLINE_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_retinex_offline.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-014.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-014.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-014.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 6s ./scripts/run_alldemo.sh --only RETINEX_OFFLINE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only TNR_CL"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    retinex_offline_standalone:
      - "RETINEX_OFFLINE no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_retinex_offline_run."
      - "Standalone page scans 100 EXDark JPEG samples and creates MEDIA_RETINEX_73."
      - "Probe reaches sample 4/100 and retinex_frames=4, showing real module processing."
      - "OpenCL kernel loads and Retinex starts/stops cleanly."
      - "Shutdown removes RETINEX_73 and destroys pool 6 and display pool 1."
    regression:
      - "TNR_CL standalone short probe continues to run with processed=60."
    assets:
      - "asset-check continues to pass after the RETINEX_OFFLINE migration."
  acceptance_limits:
    - "This stage accepts only the RETINEX_OFFLINE standalone page."
    - "The realtime RETINEX camera/bind page remains legacy proxy."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
