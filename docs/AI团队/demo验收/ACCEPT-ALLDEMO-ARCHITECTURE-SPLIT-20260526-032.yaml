acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-032"
  case_name: "ALLDEMO_STANDALONE_VPSS_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_vpss.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-032.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-032.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-032.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only VPSS"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only OSD"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    vpss_standalone:
      - "VPSS no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_vpss_run."
      - "Standalone page creates VPSS_63 with input pool 13 and output pool 14."
      - "Probe reports processed growth from 30 to 120 and ready=1111."
      - "Shutdown stops VPSS_63 and destroys pool 14, pool 13, and display pool 1."
    regression:
      - "OSD standalone short probe continues to run with module=live and processed growth."
    assets:
      - "asset-check continues to pass after the VPSS migration."
  acceptance_limits:
    - "本阶段只验收 VPSS standalone synthetic-input 多输出模块页。"
    - "不声明 VI -> VPSS -> VMIX/VO 实时绑定链路已迁移。"
