acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-025"
  case_name: "ALLDEMO_STANDALONE_CLAHE_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_clahe.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-025.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-025.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-025.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only CLAHE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only RETINEX"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    clahe_standalone:
      - "CLAHE no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_clahe_run."
      - "Standalone page creates CLAHE_69 and uses synthetic low-contrast NV12 input."
      - "Probe reports module=live, clip=2.50, grid=8x8."
      - "processed grows from 30 to 180 during the probe."
      - "Shutdown removes CLAHE_69 and destroys pool 15, pool 6, and display pool 1."
    regression:
      - "RETINEX standalone short probe continues to run with module=live."
    assets:
      - "asset-check continues to pass after the CLAHE migration."
  acceptance_limits:
    - "This stage accepts CLAHE as a standalone synthetic-input module page."
    - "It does not claim the realtime 4K camera bind path has been reimplemented outside legacy."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
