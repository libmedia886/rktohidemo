acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-035"
  case_name: "ALLDEMO_STANDALONE_WBC_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_wbc.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-035.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-035.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-035.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only WBC"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only VI"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    wbc_standalone:
      - "WBC no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_wbc_run."
      - "Standalone page creates VO_WBC_0 with WBC output pool 15."
      - "Probe reports captured growth from 29 to 179."
      - "Shutdown removes VO_WBC_0 and destroys pool 15 and display pool 1."
    regression:
      - "VI standalone short probe continues to run with VI/RESIZE_RGA/VO frame counts growing."
    assets:
      - "asset-check continues to pass after the WBC migration."
  acceptance_limits:
    - "本阶段只验收 WBC standalone VO writeback 页面。"
    - "不声明 VI + WBC + VMIX + OSD legacy 对照说明链路已迁移。"
