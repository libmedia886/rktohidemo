acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-033"
  case_name: "ALLDEMO_STANDALONE_VMIX_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_vmix.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-033.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-033.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-033.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only VMIX"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only VPSS"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    vmix_standalone:
      - "VMIX no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_vmix_run."
      - "Standalone page creates VMIX_80 with input pool 6 and output pool 7."
      - "Probe reports processed growth from 30 to 180 with 4 inputs."
      - "Shutdown removes VMIX_80 and destroys pool 7, pool 6, and display pool 1."
    regression:
      - "VPSS standalone short probe continues to run with module=live and ready=1111."
    assets:
      - "asset-check continues to pass after the VMIX migration."
  acceptance_limits:
    - "本阶段只验收 VMIX standalone synthetic-input 多输入混流模块页。"
    - "不声明 VI/VPSS -> VMIX -> OSD/VO 实时绑定链路已迁移。"
