acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-016"
  case_name: "ALLDEMO_STANDALONE_DUALVIEW_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_dualview.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-016.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-016.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-016.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --only DUALVIEW"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only CAP_DEHAZE_OFFLINE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    dualview_standalone:
      - "DUALVIEW no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_dualview_run."
      - "Standalone page creates DUALVIEW_71 and DUALVIEW_72."
      - "Both groups initialize Vulkan successfully."
      - "Probe reaches frames=60 processed=60, showing both output modes are produced."
      - "Shutdown removes both groups and destroys pool 13, pool 12, pool 11, pool 10, and display pool 1."
    regression:
      - "CAP_DEHAZE_OFFLINE standalone short probe continues to run with module=live."
    assets:
      - "asset-check continues to pass after the DUALVIEW migration."
  acceptance_limits:
    - "This stage accepts only the DUALVIEW standalone page."
    - "It does not claim remaining PAGE_IMPL_LEGACY_PROXY pages are migrated."
