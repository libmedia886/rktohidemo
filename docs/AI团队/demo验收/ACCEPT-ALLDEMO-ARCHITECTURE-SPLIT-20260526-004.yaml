acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-004"
  case_name: "ALLDEMO_ALL_PAGE_REGISTRY_AND_TILE_STATE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/alldemo.c src/app/tile_state.h src/app/tile_state.c src/app/page_registry.c src/pages/page_ops.h src/pages/page_ops.c"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 7s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --customer-demo"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only LICENSE"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    all_page_entry:
      - "HIGHLIGHT_SUPPRESS_VI covers live camera bind page."
      - "VI covers default customer loop child page."
      - "LICENSE covers non-default engineering/probe page."
    clean_shutdown:
      - "HIGHLIGHT_SUPPRESS_VI run disconnect HIGHLIGHT_SUPPRESS/RESIZE_RGA/VMIX_RGA/OSD/VO links and destroy pools."
      - "customer demo child VI run disconnect RESIZE_RGA/VMIX/OSD/VO links and destroy pools."
      - "LICENSE run initializes VO/pipeline and exits without residual process."
  acceptance_limits:
    - "This stage accepts all-page registration and shared tile state extraction only."
    - "It does not claim all concrete page implementations have been moved out of src/alldemo.c."
