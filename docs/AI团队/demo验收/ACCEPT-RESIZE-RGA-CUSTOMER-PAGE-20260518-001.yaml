acceptance_report:
  case_name: "RESIZE_RGA_CUSTOMER_PAGE"
  requirement_id: "SRS-RESIZE-RGA-CUSTOMER-PAGE-001"
  verdict: "Pass With Risks"
  evidence:
    - command: "git diff --check"
      result: "Passed"
      summary: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      summary: "重新编译和链接 build/alldemo 通过。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      summary: "循环素材检查通过，脚本路径未回归。"
    - command: "timeout -s INT -k 3s 9s stdbuf -oL -eL ./scripts/run_alldemo.sh --only RESIZE_RGA"
      result: "Passed"
      summary: "RESIZE_RGA 单页启动到真实 bind 链路；日志有多个 crop 状态、RESIZE_RGA 帧计数和 CPU/GPU/RGA 指标；生成 vo_captures/resize_rga_vo_000150.bmp；退出后无残留进程。"
    - command: "grep -c 'Width mismatch\\|bind failed\\|dynamic crop update rejected' /tmp/alldemo_resize_rga_page_srs001.log"
      result: "Passed"
      summary: "计数为 0。"
  software_issues:
    - id: "BUG-RESIZE-RGA-001"
      severity: "Major"
      category: "Runtime"
      description: "此前 RESIZE_RGA bind 模式会把 crop 宽高推断为输入端口尺寸，导致 VI 640x640 到 RESIZE_RGA 520x520 不兼容。"
      evidence: "docs/问题记录/20260518-1414-resize-rga-bind-input-width-inferred-from-crop.md"
      owner_skill: "alldemo-implementation-engineer"
      required_action: "已修复：fill_resize_attr() 显式设置 input_width/input_height 为 CAM_W/CAM_H，本轮复验错误计数为 0。"
  not_verified:
    - "未做人工屏幕视觉验收，产品仍需复看 crop 框、zoom 和输出画面是否足够直观。"
    - "未完整跑完客户默认轮播所有页面。"
  next_skill: "alldemo-product-manager"
