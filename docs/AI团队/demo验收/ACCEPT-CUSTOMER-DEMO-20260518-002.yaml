acceptance_report:
  case_name: "CUSTOMER_DEMO_ITERATION_002"
  requirement_id: "SRS-CUSTOMER-DEMO-002"
  verdict: "Pass With Risks"
  evidence:
    - command: "git diff --check"
      result: "Passed"
      summary: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      summary: "重新编译和链接 build/alldemo 通过。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      summary: "首次验证同步 lib/libmedia.a 后自动重建，素材检查通过。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      summary: "二次验证无重复同步/重建，脚本重建判断幂等。"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only VMIX"
      result: "Passed"
      summary: "VMIX 单页启动到真实 bind 链路；日志有 VI/VMIX 帧计数、CPU/GPU/RGA 指标和 alpha_layout=4；生成 vo_captures/vmix_vo_000150.bmp；退出后无残留进程。"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only RESIZE_RGA"
      result: "Passed"
      summary: "RESIZE_RGA 单页多次 crop 切换下持续输出帧；日志中 Width mismatch/bind failed 计数为 0；退出后无残留进程。"
  software_issues:
    - id: "BUG-CUSTOMER-DEMO-002-001"
      severity: "Major"
      category: "Runtime"
      description: "RESIZE_RGA bind 模式把 crop 宽高推断成输入端口尺寸，导致 VI 640x640 到 RESIZE_RGA 520x520 不兼容。"
      evidence: "docs/问题记录/20260518-1414-resize-rga-bind-input-width-inferred-from-crop.md"
      owner_skill: "alldemo-implementation-engineer"
      required_action: "已修复：fill_resize_attr() 显式设置 input_width/input_height 为 CAM_W/CAM_H。"
  not_verified:
    - "未做人工屏幕视觉验收，产品仍需复看 VMIX 页面是否足够直观。"
    - "未完整跑完客户默认轮播所有页面。"
  next_skill: "alldemo-product-manager"
