acceptance_record:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-031"
  case_name: "ALLDEMO_STANDALONE_OSD_PAGE"
  date: "2026-05-26"
  status: "Accepted By Runtime Probe"
  validation_commands:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_osd.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-031.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-031.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-031.yaml"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "cmake --build build -j"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only OSD"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only RGA"
      cwd: "/userdata/alldemo"
      result: "pass_timeout_expected"
    - command: "./scripts/run_alldemo.sh --asset-check"
      cwd: "/userdata/alldemo"
      result: "pass"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      cwd: "/userdata/alldemo"
      result: "pass_no_process"
  observed_runtime:
    osd_standalone:
      - "OSD no longer uses PAGE_IMPL_LEGACY_PROXY; it dispatches to page_osd_run."
      - "Standalone page creates OSD_60 with input pool 6 and output pool 7."
      - "Probe reports processed growth from 30 to 180 with 5 dynamic rect regions."
      - "Shutdown removes OSD_60 and destroys pool 7, pool 6, and display pool 1."
    regression:
      - "RGA standalone short probe continues to run with module=live and processed growth."
    assets:
      - "asset-check continues to pass after the OSD migration."
  acceptance_limits:
    - "本阶段只验收 OSD standalone synthetic-input 模块页。"
    - "不声明 VI -> OSD -> VMIX/VO 实时绑定链路已迁移。"
