implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-002"
  case_name: "ALLDEMO_PAGE_DESCRIPTOR_REGISTRY"
  changed_files:
    - "src/alldemo.c"
    - "src/app/page_registry.h"
    - "src/app/page_registry.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-002.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-002.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-002.yaml"
  implementation_scope:
    - "新增 page_desc_t，字段包括 name、flow_note、showcase_note、flags、bind_display。"
    - "新增 PAGE_FLAG_CAMERA、PAGE_FLAG_CAMERA_4K、PAGE_FLAG_CAMERA_1080P、PAGE_FLAG_VMIX_OSD_DISPLAY。"
    - "新增 PAGE_BIND_* 枚举，用一个页面属性替代 main 中一组 bind-display 名称判断。"
    - "把 module_flow_note/module_showcase_note 的页面文案移入 page_registry。"
    - "tile_needs_camera/tile_uses_4k_camera_input/tile_uses_1080p_camera_input 改为读取 page_desc flags。"
    - "src/alldemo.c 的 use_*_bind_display 变量改为由 page_bind_display 派生。"
  remaining_alldemo_c_scope:
    - "页面具体实现函数仍在 src/alldemo.c。"
    - "下一阶段可选择 HIGHLIGHT_SUPPRESS_VI 或 VI 页面，迁移到 src/pages/page_*.c，验证完整页面模块化。"
  validation:
    - command: "git diff --check -- src/alldemo.c src/app/page_registry.h src/app/page_registry.c"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "timeout -s INT -k 3s 7s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "启动 VI -> HIGHLIGHT_SUPPRESS -> RESIZE_RGA -> VMIX_RGA -> OSD -> VO；帧计数到 vi=180/suppress=180/resize=180；passthrough 在 0/1 间切换；退出清理完成。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --customer-demo"
      result: "Passed By Timeout Probe"
      evidence: "customer demo loop 启动 --only VI 子页，VI 帧计数到 119，退出清理完成。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  known_risks:
    - "page_desc 目前仍是 C 表，后续可以继续拆成每个 page 自己注册，减少中心表冲突。"
  next_skill: "alldemo-software-acceptance"
