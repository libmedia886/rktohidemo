implementation_summary:
  requirement_id: "SRS-OSD-CUSTOMER-PAGE-001"
  case_name: "OSD_CUSTOMER_PAGE"
  changed_files:
    - "src/alldemo.c"
    - "README.md"
    - "assets/effect_manifest.json"
    - "docs/AI团队/demo需求/PRODUCT-OSD-CUSTOMER-PAGE.yaml"
    - "docs/AI团队/demo需求/SRS-OSD-CUSTOMER-PAGE-001.yaml"
    - "docs/AI团队/demo实现/IMPL-OSD-CUSTOMER-PAGE-20260518-001.yaml"
  implementation_scope:
    - "保持 OSD 在客户默认页表中，不改变当前默认顺序。"
    - "保持现有 OSD 实时 bind 链路：VI -> LIVE_OSD -> VMIX -> DISPLAY_OSD -> VO。"
    - "将 OSD 加入 VO 抓帧路径，生成 vo_captures/osd_vo_*.bmp。"
    - "更新 README，说明第一级 live OSD 和第二级页面 OSD 的分工。"
    - "更新 effect manifest，描述两级 OSD 动态 region、zorder、alpha、enabled 和抓帧证据。"
  non_scope:
    - "不修改 /userdata/rktohi 源码。"
    - "不修改 MEDIA_OSD API 或底层 OSD 模块。"
    - "不重新设计 OSD 页面布局和 region 动画参数。"
    - "不声明已经完成人工视觉验收。"
  data_flow: "VI_0.output -> OSD_60.input; OSD_60.output0 -> VMIX_80.input0; VMIX_80.output0 -> OSD_81.input; OSD_81.output0 -> VO_0.input0"
  user_visible_changes:
    - "README 中 OSD 页面说明更明确：live OSD 叠加动态目标框、扫描线、状态条和告警块，页面 OSD 叠加中文数据流、参数说明和指标。"
    - "OSD 页面运行时会保存 vo_captures/osd_vo_000150.bmp 等抓帧，方便后续产品复看。"
    - "manifest 中 OSD 不再只是泛化 HUD 描述，而是明确动态 region 和两级 OSD。"
  validation:
    - command: "git diff --check"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL、TRANSFORM、VMIX、AVM 素材检查通过。"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only OSD"
      result: "Passed"
      evidence: "/tmp/alldemo_osd_srs001.log 记录 VI -> OSD_60 -> VMIX_80 -> OSD_81 -> VO 的 bind 链路，OSD 帧计数增长，生成 osd_vo_000150.bmp，退出后 pgrep 无残留。"
  not_verified:
    - "未由现场人员看屏幕或抓帧确认 OSD 页面视觉效果。"
    - "未完整跑完全部客户默认轮播周期。"
  known_risks:
    - "OSD 页面布局未重做，本轮只增强说明准确性和抓帧证据；产品复看后可能继续要求调整文字密度或 region 位置。"
  next_skill: "alldemo-software-acceptance"
