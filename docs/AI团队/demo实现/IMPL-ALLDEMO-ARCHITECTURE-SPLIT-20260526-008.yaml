implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-008"
  case_name: "ALLDEMO_LIGHTWEIGHT_MAIN_AND_LEGACY_FALLBACK"
  changed_files:
    - "CMakeLists.txt"
    - "src/alldemo.c"
    - "src/legacy/alldemo_legacy.c"
    - "src/legacy/alldemo_legacy.h"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-008.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-008.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-008.yaml"
  implementation_scope:
    - "原 src/alldemo.c 移动为 src/legacy/alldemo_legacy.c，并将 main 改名为 alldemo_legacy_main。"
    - "新增 src/legacy/alldemo_legacy.h，供轻量主入口调用 legacy fallback。"
    - "新 src/alldemo.c 只包含 signal handler、parse_only_tile、page_ops_find、find_tile_index 和 fallback 调用。"
    - "--only standalone 页面在新入口直接 run；其他参数和 legacy 页面转入 alldemo_legacy_main。"
    - "CMakeLists.txt 增加 src/legacy/alldemo_legacy.c。"
  validation:
    - command: "git diff --check -- CMakeLists.txt src/alldemo.c src/legacy/alldemo_legacy.c src/legacy/alldemo_legacy.h src/pages/page_ops.c src/pages/page_info.c src/pages/page_surface.c src/pages/page_license.c"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "wc -l src/alldemo.c src/legacy/alldemo_legacy.c"
      result: "Passed"
      evidence: "src/alldemo.c 为 45 行；src/legacy/alldemo_legacy.c 为 16553 行。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only VENC"
      result: "Passed By Timeout Probe"
      evidence: "新入口分发到 VENC standalone info page；info frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only LICENSE"
      result: "Passed By Timeout Probe"
      evidence: "新入口分发到 LICENSE standalone page；frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "fallback 到 legacy live 页面；HIGHLIGHT_SUPPRESS_VI 帧计数到 vi=119/suppress=119/resize=119。"
    - command: "timeout -s INT -k 3s 4s ./scripts/run_alldemo.sh --customer-demo"
      result: "Passed By Timeout Probe"
      evidence: "customer loop 启动 --only VI 子页，VI/resize 帧计数到约 90，退出清理完成。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  remaining_work:
    - "legacy fallback 仍包含具体页面大实现；下一阶段继续按页面类型拆出真正 page_xxx.c。"
    - "优先拆不依赖 camera/bind 的 offline/showcase 页面，再拆 live camera/bind 页面。"
  next_skill: "alldemo-software-acceptance"
