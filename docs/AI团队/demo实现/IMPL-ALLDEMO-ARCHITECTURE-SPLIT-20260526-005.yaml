implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-005"
  case_name: "ALLDEMO_FIRST_STANDALONE_PAGE"
  changed_files:
    - "CMakeLists.txt"
    - "src/alldemo.c"
    - "src/pages/page_ops.h"
    - "src/pages/page_ops.c"
    - "src/pages/page_license.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-005.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-005.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-005.yaml"
  implementation_scope:
    - "page_ops_t 增加 impl 和 run 字段，支持 PAGE_IMPL_STANDALONE。"
    - "page_ops_find 对 LICENSE 返回 standalone ops，其他已登记页面继续返回 PAGE_IMPL_LEGACY_MAIN。"
    - "src/alldemo.c 在 --only 校验后优先分发 standalone 页面。"
    - "新增 src/pages/page_license.c，独立完成 MEDIA_SYS_Init、MEDIA_SYS_SetLicense、MEDIA_POOL_Create、VO 创建/启动、NV12 页面绘制和 MEDIA_SYS_SendFrame。"
    - "LICENSE 页面按 30fps 送帧，日志输出 readable 状态和 frame 计数，退出时停止 VO、销毁 pool、退出 MEDIA_SYS。"
    - "CMakeLists.txt 将 src/pages/page_license.c 编入 alldemo。"
  validation:
    - command: "git diff --check -- CMakeLists.txt src/alldemo.c src/pages/page_ops.h src/pages/page_ops.c src/pages/page_license.c src/app/page_registry.c src/app/tile_state.c src/app/tile_state.h"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only LICENSE"
      result: "Passed By Timeout Probe"
      evidence: "打印 LICENSE standalone page path=/root/licence.dat readable=1；frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 7s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "HIGHLIGHT_SUPPRESS_VI 帧计数到 vi=150/suppress=150/resize=150；passthrough 0/1 切换；退出清理完成。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --customer-demo"
      result: "Passed By Timeout Probe"
      evidence: "customer demo loop 启动 --only VI 子页，VI/resize 帧计数到 119。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  remaining_work:
    - "LICENSE 已成为第一个独立页面；其他页面仍通过 legacy main 运行。"
    - "下一阶段可按页面复杂度逐个迁移，优先选择 HIGHLIGHT_SUPPRESS_VI 或 VI 这类当前已验证的 live 页面。"
  next_skill: "alldemo-software-acceptance"
