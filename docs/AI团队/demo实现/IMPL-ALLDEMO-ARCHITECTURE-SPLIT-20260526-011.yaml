implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-011"
  case_name: "ALLDEMO_STANDALONE_HIGHLIGHT_SUPPRESS_PAGE"
  changed_files:
    - "CMakeLists.txt"
    - "src/pages/page_ops.c"
    - "src/pages/page_highlight_suppress.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-011.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-011.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-011.yaml"
  implementation_scope:
    - "新增 page_highlight_suppress.c，使用 page_surface_open/page_surface_send_frame/page_surface_close 独立运行页面。"
    - "页面生成合成 NV12 高光输入，调用 MEDIA_HIGHLIGHT_SUPPRESS 处理后绘制 input/output 上下对比。"
    - "使用 MEDIA_POOL 6/7 作为页面私有输入/输出工作池，匹配 legacy 已验证的 pool 范围。"
    - "读取 MEDIA_HIGHLIGHT_SUPPRESS_GetLastPerf，显示 GPU kernel/queue 或 CPU perf。"
    - "page_ops.c 将 HIGHLIGHT_SUPPRESS 注册为 PAGE_IMPL_STANDALONE/page_highlight_suppress_run。"
    - "CMakeLists.txt 增加 src/pages/page_highlight_suppress.c。"
  validation:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_highlight_suppress.c src/pages/page_ops.c docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-011.yaml docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-011.yaml docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-011.yaml"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "timeout -s INT -k 3s 6s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS"
      result: "Passed By Timeout Probe"
      evidence: "HIGHLIGHT_SUPPRESS standalone page module=live；frames=120 processed=120；gpu=1 kernel/queue 有有效数值；退出后销毁 module 和 pool 7/6/1。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "legacy live 页面仍可运行，帧计数到 vi=120/suppress=120/resize=119，bypass 状态切换。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only VO"
      result: "Passed By Timeout Probe"
      evidence: "VO standalone page；frames 到 60；退出时恢复 freeze/hide 状态。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  fix_notes:
    - "首次探测时使用 pool 30/31 创建失败，日志为 pool_id或count无效。"
    - "修正为 legacy 合成页已验证的 pool 6/7 后，HIGHLIGHT_SUPPRESS 模块进入 live 路径，processed 随 frames 增长。"
  remaining_work:
    - "HIGHLIGHT_SUPPRESS 已从 legacy proxy 迁出；HIGHLIGHT_SUPPRESS_VI 仍留在 legacy proxy，后续可单独迁移 camera/bind 运行时。"
    - "其余 PAGE_IMPL_LEGACY_PROXY 页面仍需逐步替换为 standalone。"
  next_skill: "alldemo-software-acceptance"
