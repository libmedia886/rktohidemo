implementation_summary:
  requirement_id: "SRS-RESIZE-RGA-CUSTOMER-PAGE-001"
  case_name: "RESIZE_RGA_CUSTOMER_PAGE"
  changed_files:
    - "src/alldemo.c"
    - "README.md"
    - "assets/effect_manifest.json"
    - "docs/AI团队/demo需求/PRODUCT-RESIZE-RGA-CUSTOMER-PAGE.yaml"
    - "docs/AI团队/demo需求/SRS-RESIZE-RGA-CUSTOMER-PAGE-001.yaml"
    - "docs/AI团队/demo实现/IMPL-RESIZE-RGA-CUSTOMER-PAGE-20260518-001.yaml"
  implementation_scope:
    - "保持 RESIZE_RGA 在客户默认页表中，不改变当前默认顺序。"
    - "保持现有 RESIZE_RGA 实时 bind 链路：VI -> RESIZE_RGA -> VMIX -> DISPLAY_OSD -> VO。"
    - "将 RESIZE_RGA 加入 VO 抓帧路径，生成 vo_captures/resize_rga_vo_*.bmp。"
    - "在 RESIZE_RGA 单页主循环中调用抓帧函数。"
    - "保持 input_width/input_height 为 CAM_W/CAM_H，crop_w/crop_h 只作为源裁剪窗口。"
    - "更新 README，说明 crop x/y/w/h、zoom、RESIZE_RGA 帧计数、运行指标和抓帧证据。"
    - "更新 effect manifest，描述动态 crop window、硬件 scale/zoom 和 VO 抓帧。"
  non_scope:
    - "不修改 /userdata/rktohi 源码。"
    - "不修改 MEDIA_RESIZE_RGA API 或底层模块。"
    - "不重新设计 RESIZE_RGA 页面布局和 crop 动画参数。"
    - "不声明已经完成人工视觉验收。"
  data_flow: "VI_0.output -> RESIZE_RGA_61.input0; RESIZE_RGA_61.output0 -> VMIX_80.input0; VMIX_80.output0 -> OSD_81.input; OSD_81.output0 -> VO_0.input0"
  user_visible_changes:
    - "README 中 RESIZE_RGA 页面说明更明确：页面动态移动 crop 框、显示 zoom、帧计数和指标。"
    - "RESIZE_RGA 页面运行时会保存 vo_captures/resize_rga_vo_000150.bmp 等抓帧，方便后续产品复看。"
    - "manifest 中 RESIZE_RGA 不再只是泛化 hardware resize tiles 描述，而是明确动态 crop 和硬件缩放。"
  validation:
    - command: "git diff --check"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL、TRANSFORM、VMIX、AVM 素材检查通过。"
    - command: "timeout -s INT -k 3s 9s stdbuf -oL -eL ./scripts/run_alldemo.sh --only RESIZE_RGA"
      result: "Passed"
      evidence: "/tmp/alldemo_resize_rga_page_srs001.log 记录 VI -> RESIZE_RGA_61 -> VMIX_80 -> OSD_81 -> VO 的 bind 链路，多个 crop 状态有帧计数，生成 resize_rga_vo_000150.bmp，退出后 pgrep 无残留。"
  not_verified:
    - "未由现场人员看屏幕或抓帧确认 RESIZE_RGA 页面视觉效果。"
    - "未完整跑完全部客户默认轮播周期。"
  known_risks:
    - "RESIZE_RGA 页面布局未重做，本轮只增强说明准确性和抓帧证据；产品复看后可能继续要求调整 crop 示意框或 zoom 文案。"
  next_skill: "alldemo-software-acceptance"
