implementation_summary:
  requirement_id: "SRS-CUSTOMER-DEMO-002"
  case_name: "CUSTOMER_DEMO_ITERATION_002"
  changed_files:
    - "src/alldemo.c"
    - "README.md"
    - "assets/effect_manifest.json"
    - "scripts/run_alldemo.sh"
    - "lib/libmedia.a"
    - "docs/AI团队/demo需求/PRODUCT-CUSTOMER-DEMO-ITERATION-002.yaml"
    - "docs/AI团队/demo需求/SRS-CUSTOMER-DEMO-002.yaml"
    - "docs/问题记录/20260518-1414-resize-rga-bind-input-width-inferred-from-crop.md"
    - "docs/问题记录/INDEX.md"
  implementation_scope:
    - "将 VMIX 加入客户默认轮播、工程页表和 README 的 --only 示例。"
    - "新增 VMIX 展示布局：四路 VPSS 输出绑定到 VMIX input0..3，使用不同位置和 alpha 做实时合成。"
    - "新增 VMIX OSD 展示：四路输入边框、alpha 标签、中文数据流、合成原因、VI/VMIX 帧计数和 CPU/GPU/RGA 状态。"
    - "VMIX 单页周期抓取 VO 输出帧，输出到 vo_captures/vmix_vo_*.bmp。"
    - "修复 RESIZE_RGA bind 输入尺寸，显式设置 input_width/input_height 为 CAM_W/CAM_H。"
    - "run_alldemo.sh 在 src、CMakeLists.txt、include/media_api.h 或 lib/libmedia.a 更新时自动重新构建。"
    - "更新 README 和 effect manifest，使 VMIX 说明从工程占位描述变成真实硬件合成/alpha 展示。"
  non_scope:
    - "不修改 /userdata/rktohi 源码。"
    - "不改变 VMIX 或 RESIZE_RGA 底层库 API。"
    - "不重新设计其它客户演示页面。"
    - "不声明已经完成现场人工视觉验收。"
  data_flow:
    vmix: "VI0.output -> VPSS63.input; VPSS output0..3 -> VMIX80 input0..3; VMIX80.output0 -> OSD81.input; OSD81.output0 -> VO0.input0"
    resize_rga: "VI0.output -> RESIZE_RGA61.input0; RESIZE_RGA61.output0 -> VMIX80.input0; VMIX80.output0 -> OSD81.input; OSD81.output0 -> VO0.input0"
  user_visible_changes:
    - "默认客户演示页表现在包含 VMIX，顺序为 VI、VPSS、VMIX、OSD..."
    - "VMIX 页面显示四路实时输入的叠放、层级和 alpha 混合，而不是只作为工程清单项。"
    - "RESIZE_RGA 页面在默认轮播中可以继续输出帧，不再因输入尺寸不匹配提前退出。"
    - "README 中有 /userdata/alldemo/scripts/run_alldemo.sh --only VMIX 的直接运行示例和中文链路说明。"
  validation:
    - command: "git diff --check"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "第一次运行同步 lib/libmedia.a 并重新链接；THERMAL、TRANSFORM、VMIX、AVM 素材检查通过。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "第二次运行没有再次同步或重建，素材检查仍通过。"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only VMIX"
      result: "Passed"
      evidence: "/tmp/alldemo_vmix_srs002.log 记录 VPSS output0..3 -> VMIX input0..3、VMIX 帧计数、CPU/GPU/RGA 指标和 vmix_vo_000150.bmp 抓帧；退出后 pgrep 无残留。"
    - command: "timeout -s INT -k 3s 8s ./scripts/run_alldemo.sh --only RESIZE_RGA"
      result: "Passed"
      evidence: "/tmp/alldemo_resize_rga_srs002.log 记录 RESIZE_RGA 多个 crop 状态均有输出帧；Width mismatch/bind failed 计数为 0；退出后 pgrep 无残留。"
  not_verified:
    - "未由现场人员逐页观看并确认 VMIX 页面视觉效果是否达到产品主观标准。"
    - "未完整跑完全部客户默认轮播周期。"
  known_risks:
    - "VMIX 的画面美观性和客户理解度仍需要产品复看；当前只完成软件层面的页面闭环和运行验证。"
  next_skill: "alldemo-software-acceptance"
