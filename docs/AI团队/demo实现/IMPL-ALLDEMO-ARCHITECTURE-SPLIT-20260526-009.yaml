implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-009"
  case_name: "ALLDEMO_ALL_PAGE_OPS_RUN_ENTRIES"
  changed_files:
    - "CMakeLists.txt"
    - "src/alldemo.c"
    - "src/pages/page_ops.h"
    - "src/pages/page_ops.c"
    - "src/pages/page_legacy_proxy.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-009.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-009.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-009.yaml"
  implementation_scope:
    - "page_impl_kind_t 新增 PAGE_IMPL_LEGACY_PROXY，用于明确未迁移页面是代理到 legacy，不再是主入口 fallback。"
    - "新增 page_legacy_proxy.c，提供 VI、VPSS、VO、WBC、RGA、RESIZE_RGA、CSC_RGA、CSC_CL、OSD、CLAHE、RETINEX、RETINEX_OFFLINE、TNR_CL、HIGHLIGHT_SUPPRESS、HIGHLIGHT_SUPPRESS_VI、EIS、EIS_VI、CAP_DEHAZE、CAP_DEHAZE_OFFLINE、DCP_FAST_DEHAZE、THERMAL、CONV_CL、TRANSFORM、BLEND_PYR、EDOF_CL、MCF_FUSION_CL、DUALVIEW、STEREO_3D、VMIX、PANO、AVM2D、NPU 的 legacy run 函数。"
    - "page_ops.c 将全部已登记页面纳入 g_page_ops；standalone info/special 页面保持独立，其他页面走 legacy proxy。"
    - "src/alldemo.c 不再只执行 PAGE_IMPL_STANDALONE，而是执行所有带 run 的 page_ops。"
    - "CMakeLists.txt 增加 src/pages/page_legacy_proxy.c。"
  validation:
    - command: "git diff --check -- CMakeLists.txt src/alldemo.c src/pages/page_ops.h src/pages/page_ops.c src/pages/page_legacy_proxy.c src/legacy/alldemo_legacy.c src/legacy/alldemo_legacy.h"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "timeout -s INT -k 3s 4s ./scripts/run_alldemo.sh --only VI"
      result: "Passed By Timeout Probe"
      evidence: "page_ops legacy proxy 进入 VI 页面；VI/resize 帧计数到约 90；退出清理完成。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only VENC"
      result: "Passed By Timeout Probe"
      evidence: "VENC standalone info page；info frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "legacy proxy 进入 HIGHLIGHT_SUPPRESS_VI；帧计数到 vi=119/suppress=119/resize=119。"
    - command: "timeout -s INT -k 3s 4s ./scripts/run_alldemo.sh --customer-demo"
      result: "Passed With Forced-Timeout Cleanup Warning"
      evidence: "customer loop 启动 --only VI 子页，VI/resize 帧计数到约 89；强制中断时观察到 pool_id=6 buffer 未归还警告，但进程退出且无残留。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  remaining_work:
    - "所有页面已有 page_ops run 入口，但多数页面仍是 legacy proxy。"
    - "下一阶段要按页面类型逐步把 legacy proxy 替换为真正 standalone 实现。"
  next_skill: "alldemo-software-acceptance"
