implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-010"
  case_name: "ALLDEMO_STANDALONE_VO_PAGE"
  changed_files:
    - "CMakeLists.txt"
    - "src/pages/page_ops.c"
    - "src/pages/page_vo.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-010.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-010.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-010.yaml"
  implementation_scope:
    - "新增 page_vo.c，使用 page_surface_open/page_surface_send_frame/page_surface_close 独立创建 VO 显示页面。"
    - "实现 VO 状态机：NORMAL、FREEZE MAIN、NORMAL、FREEZE PLANE、NORMAL、HIDE WARNING、HIDE PLANE。"
    - "状态变化后调用 MEDIA_VO_FreezeMain、MEDIA_VO_UnfreezeMain、MEDIA_VO_FreezePlane、MEDIA_VO_HidePlane。"
    - "页面退出前调用 vo_restore_output，确保 freeze/hide 状态恢复。"
    - "page_ops.c 将 VO 注册为 PAGE_IMPL_STANDALONE/page_vo_run。"
    - "CMakeLists.txt 增加 src/pages/page_vo.c。"
  validation:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_vo.c src/pages/page_ops.c src/pages/page_ops.h src/pages/page_surface.c src/pages/page_surface.h src/alldemo.c"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "timeout -s INT -k 3s 7s ./scripts/run_alldemo.sh --only VO"
      result: "Passed By Timeout Probe"
      evidence: "VO standalone page；frames 到 180；调用 MEDIA_VO_FreezeMain(1) 并恢复。"
    - command: "timeout -s INT -k 3s 20s ./scripts/run_alldemo.sh --only VO"
      result: "Passed By Timeout Probe"
      evidence: "VO frames 覆盖 NORMAL、FREEZE MAIN、FREEZE PLANE、HIDE WARNING、HIDE PLANE；日志显示 freeze/hide 恢复调用。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "legacy live 页面仍可运行，帧计数到 vi=120/suppress=119/resize=119。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  remaining_work:
    - "VO 已从 legacy proxy 迁出；其余 PAGE_IMPL_LEGACY_PROXY 页面仍需逐步替换为 standalone。"
    - "下一阶段可继续迁移不依赖 camera bind 的 synthetic/offline 页，或先拆 camera/bind 公共运行时。"
  next_skill: "alldemo-software-acceptance"
