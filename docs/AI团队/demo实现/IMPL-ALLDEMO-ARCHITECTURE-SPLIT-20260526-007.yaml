implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-007"
  case_name: "ALLDEMO_STANDALONE_INFO_PAGES"
  changed_files:
    - "CMakeLists.txt"
    - "src/pages/page_ops.c"
    - "src/pages/page_info.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-007.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-007.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-007.yaml"
  implementation_scope:
    - "新增 page_info.c，复用 page_surface_open/page_surface_send_frame/page_surface_close。"
    - "page_info_run_named 读取 page_desc_find(name)，console 输出 flow_note 和 showcase_note。"
    - "page_info 绘制 ASCII 页面名、STANDALONE PAGE、MEDIA API INFO、PROBE CONTRACT 和 frame counter。"
    - "page_ops.c 新增 AVM、SVM3D、VMIX_RGA、EXPOSURE_FUSION_CL、VENC、VDEC、RTSP_SEND、RTSP_RECV、PIC_IO 的 standalone ops。"
    - "CMakeLists.txt 增加 src/pages/page_info.c。"
  validation:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_ops.c src/pages/page_info.c src/pages/page_surface.c src/pages/page_surface.h src/pages/page_license.c"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only VENC"
      result: "Passed By Timeout Probe"
      evidence: "VENC standalone info page；打印 VENC flow/showcase；info frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only RTSP_SEND"
      result: "Passed By Timeout Probe"
      evidence: "RTSP_SEND standalone info page；打印 RTSP_SEND flow/showcase；info frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only LICENSE"
      result: "Passed By Timeout Probe"
      evidence: "LICENSE standalone page path=/root/licence.dat readable=1；frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed With Forced-Timeout Cleanup Warning"
      evidence: "HIGHLIGHT_SUPPRESS_VI 帧计数到 vi=119/suppress=119/resize=119；强制中断时观察到 pool_id=15 buffer 未归还警告，但进程退出且 pipeline 清理完成。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  remaining_work:
    - "info 页面是工程入口页，不等同于真实 VENC/VDEC/RTSP/AVM/SVM3D 算法链路验收。"
    - "下一阶段可拆 camera/bind 公共上下文，用于迁移 VI 或 HIGHLIGHT_SUPPRESS_VI。"
  next_skill: "alldemo-software-acceptance"
