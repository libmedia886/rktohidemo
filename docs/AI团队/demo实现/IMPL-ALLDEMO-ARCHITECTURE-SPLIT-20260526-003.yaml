implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-003"
  case_name: "ALLDEMO_PAGE_RUNTIME_CONTEXT"
  changed_files:
    - "CMakeLists.txt"
    - "src/alldemo.c"
    - "src/app/page_runtime.h"
    - "src/app/page_runtime.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-003.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-003.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-003.yaml"
  implementation_scope:
    - "新增 page_runtime_t，集中承载 active、need_camera、use_4k_camera_input、use_1080p_camera_input、bind_display 和 use_*_bind_display 状态。"
    - "新增 page_runtime_init()，从 page_registry 派生运行配置。"
    - "src/alldemo.c 在 main 开始处初始化 page_rt，并从 page_rt 派生 camera 输入尺寸。"
    - "src/alldemo.c 的 bind-display 开关改为读取 page_rt 字段。"
    - "CMakeLists.txt 增加 src/app/page_runtime.c。"
  validation:
    - command: "grep -n \"page_bind_display_t bind_display\\|tile_uses_4k_camera_input(only_tile)\\|tile_uses_1080p_camera_input(only_tile)\\|tile_needs_camera(only_tile)\\|page_has_flag(only_tile\" src/alldemo.c"
      result: "Passed"
      evidence: "exit code 1，main 中不再直接派生这些页面运行配置。"
    - command: "git diff --check -- CMakeLists.txt src/alldemo.c src/app/page_runtime.h src/app/page_runtime.c src/app/page_registry.h src/app/page_registry.c"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "timeout -s INT -k 3s 7s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "HIGHLIGHT_SUPPRESS_VI 帧计数到 vi=180/suppress=180/resize=180；passthrough 在 0/1 间切换；退出清理完成。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --customer-demo"
      result: "Passed By Timeout Probe"
      evidence: "customer demo loop 启动 --only VI 子页，VI 帧计数到 119，退出清理完成。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  known_risks:
    - "page_runtime 当前仍输出较多 use_* 字段，是兼容现有 main 的过渡层；后续迁移 pages/page_*.c 后可收敛为 per-page ops。"
  next_skill: "alldemo-software-acceptance"
