implementation_summary:
  requirement_id: "SRS-ALLDEMO-ARCHITECTURE-SPLIT-006"
  case_name: "ALLDEMO_STANDALONE_PAGE_SURFACE"
  changed_files:
    - "CMakeLists.txt"
    - "src/pages/page_surface.h"
    - "src/pages/page_surface.c"
    - "src/pages/page_license.c"
    - "docs/AI团队/demo需求/SRS-ALLDEMO-ARCHITECTURE-SPLIT-006.yaml"
    - "docs/AI团队/demo实现/IMPL-ALLDEMO-ARCHITECTURE-SPLIT-20260526-006.yaml"
    - "docs/AI团队/demo验收/ACCEPT-ALLDEMO-ARCHITECTURE-SPLIT-20260526-006.yaml"
  implementation_scope:
    - "新增 page_surface_t，记录 standalone 页面显示面的 pool、尺寸、stride、fps、frame_size 和初始化状态。"
    - "新增 page_surface_open/page_surface_close，统一处理 MEDIA_SYS_Init、MEDIA_SYS_SetLicense、MEDIA_POOL_Create、MEDIA_VO_SetAttr/CreateChn/Start/Stop/Destroy。"
    - "新增 page_surface_send_frame，统一获取 buffer、CPU 写同步、mmap 回退、调用页面 draw callback、送入 VO。"
    - "新增 page_surface_fill_rect_nv12 和 page_surface_draw_text，沉淀简单页面绘制能力。"
    - "page_license.c 删除本地媒体初始化、送帧、NV12 rect/font 代码，改为调用 page_surface。"
    - "CMakeLists.txt 增加 src/pages/page_surface.c。"
  validation:
    - command: "git diff --check -- CMakeLists.txt src/pages/page_surface.h src/pages/page_surface.c src/pages/page_license.c src/pages/page_ops.h src/pages/page_ops.c src/alldemo.c"
      result: "Passed"
      evidence: "无 whitespace error。"
    - command: "cmake --build build -j"
      result: "Passed"
      evidence: "[100%] Built target alldemo。"
    - command: "timeout -s INT -k 3s 3s ./scripts/run_alldemo.sh --only LICENSE"
      result: "Passed By Timeout Probe"
      evidence: "LICENSE standalone page path=/root/licence.dat readable=1；frames 到 60；pool/pipeline clean shutdown。"
    - command: "timeout -s INT -k 3s 5s ./scripts/run_alldemo.sh --only HIGHLIGHT_SUPPRESS_VI"
      result: "Passed By Timeout Probe"
      evidence: "HIGHLIGHT_SUPPRESS_VI 帧计数到 vi=119/suppress=119/resize=119；passthrough 0/1 切换；退出清理完成。"
    - command: "timeout -s INT -k 3s 4s ./scripts/run_alldemo.sh --customer-demo"
      result: "Passed With Forced-Timeout Cleanup Warning"
      evidence: "customer demo loop 启动 --only VI 子页，VI/resize 帧计数到 89；强制中断清理期间观察到 pool_id=6 buffer 未归还警告，但无残留进程。"
    - command: "./scripts/run_alldemo.sh --asset-check"
      result: "Passed"
      evidence: "THERMAL 2/2、TRANSFORM 1/1、VMIX 2/2、CAP_DEHAZE_OFFLINE 3/3、AVM 4/4、RETINEX_OFFLINE 100/100、AVM2D 25/25。"
    - command: "pgrep -af '/userdata/alldemo/.*/alldemo|build/alldemo|run_alldemo.sh'"
      result: "Passed"
      evidence: "exit code 1，无残留进程。"
  remaining_work:
    - "page_surface 适用于简单 standalone 页面；camera/bind 类页面仍需要进一步拆 live page runtime。"
    - "后续迁移 HIGHLIGHT_SUPPRESS_VI/VI 时，应先拆 camera + bind + overlay 更新上下文。"
  next_skill: "alldemo-software-acceptance"
